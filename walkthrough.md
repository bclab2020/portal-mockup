# 顔rPPG＆骨格メイク分析統合完了ウォークスルー

ご要望の顔診断＆rPPG血流スキャンアプリの開発、およびポータルサイトへの統合がすべて完了しました。

## 変更・作成されたファイル
1. **新規作成** [face_analyzer.html](file:///C:/Users/ke/.gemini/antigravity/scratch/connect-ai-v2/face_analyzer.html) (顔診断＆rPPGアプリ本体)
2. **ポータル統合** [index.html](file:///C:/Users/ke/.gemini/antigravity/scratch/connect-ai-v2/index.html) (顔診断用カード、タブ切り替え、レポートカード追加)
3. **統合ロジック** [portal.js](file:///C:/Users/ke/.gemini/antigravity/scratch/connect-ai-v2/portal.js) (iframe切り替え、postMessageデータ処理、コスメ連動広告、タイムライン自動返信)
4. **アップローダー** [deploy_to_github.py](file:///C:/Users/ke/.gemini/antigravity/scratch/connect-ai-v2/deploy_to_github.py) (アップロード対象に `face_analyzer.html` を追加)

---

## 搭載された主要機能・統合連携ワークフロー

### 1. タブ・リンクからのシームレスな起動
- **美容骨格タブ（上部固定バナー）:** 画面最上部に「✨ AI 顔骨格・メイク＆rPPG血流測定ラボ」の特別案内カードが**常時固定でピン留め**されました。**「📸 AI顔アライメント・メイク測定を起動する」**ボタンを押すと、測定ラボタブに切り替わり、自動的に顔診断モードが起動します。
- **美容骨格記事本文内からの誘導（プラスアルファ導線）:** 
  - **「顔面非対称（Facial Asymmetry）」記事:** 論文概要の下に「顔の左右の対称性を数値化してみましょう」という誘導枠を追加し、直接顔測定を起動するボタンを並列で配置しました。
  - **「デコルテを美しく見せる」記事:** 「首のねじれは顔の巡り（くすみ）にも直結します」という顔血流測定の親和性を説明する誘導枠を追加し、同じく測定起動ボタンを追加しました。
- **測定ラボタブ:** iframeの上部に「🧍 身体姿勢測定」と「✨ 顔骨格・rPPGメイク測定」の切り替えボタンが配置されました。ここから手動でいつでも切り替えることができます。

### 2. ポータル側診断レポートとの動的連動
- 顔診断アプリで測定を完了し、**「ポータルに結果を送信」**をクリックすると、ポータル側のレポート枠が自動的に「📊 美容顔アライメント＆健康診断レポート」へと切り替わります。
- 測定結果（顔の骨格型、左右対称性、rPPG心拍数、自律神経ストレス値、および選択したなりたいイメージに沿ったメイクアドバイス）がポータル内に美しく反映されます。

### 3. データ連動型・化粧品マッチング広告 (Dynamic Ads)
- 測定した「なりたい顔」に連動し、ポータルのサイドバー広告枠が自動的におすすめ化粧品（20%OFFクーポンコード付き）に切り替わります。
  - **キュート:** ELIXIR チークパウダー (ピーチピンク)
  - **クール:** KATE 3Dコントゥアリングパウダー (シェーディング)
  - **エレガント:** ELIXIR デザインタイムセラム (リフトアップ美容液)

### 4. コミュニティへの測定添付＆アドバイザーの自動回答
- 測定結果をポータルのSNS共有やコミュニティタイムラインへ直接添付して相談投稿できます。
- コミュニティへ顔アライメントデータを添付して投稿すると、3秒後に**「Rin (ビューティアドバイザー)」**から、顔立ちやリンパマッサージ、コスメに関するアドバイスが自動返信されます。

---

## 動作確認・検証手順

現在、ローカルサーバーがポート `8000` で稼働しています。

1. **ポータルのトップページを開きます:**
   [http://localhost:8000/index.html](http://localhost:8000/index.html)
2. **美容骨格タブへアクセス:**
   上部メニューの「✨ 美容骨格」タブを選択し、一番上の「AI 顔骨格・メイク＆rPPG血流測定ラボ」カードの「AI顔アライメント・メイク測定を起動する」をクリックします。
3. **カメラの起動と測定:**
   測定ラボが開き、自動的に「顔骨格・rPPGバイタル測定」がロードされます。「📷 カ*   6.  **呼吸ガイドのゲーミフィケーション（風船アニメーション） [NEW]**
    *   **呼吸同調バルーン**: 4-7-8呼吸法（吸う4秒・止める7秒・吐く8秒）およびリラックス呼吸（吸う3秒・吐く6秒）のタイミングに合わせ、鼓動するように膨張・収縮する**動的ネオンサークル**を開発・実装。
    *   **コントロールボタン（開始・一時停止）**: ユーザーがストレッチや瞑想の準備をしてから自分のタイミングで呼吸を合わせられるよう、バルーンの下部に「▶ ガイドを開始する」「⏸ 一時停止」の操作トグルボタンを実装。デフォルトは一時停止（初期状態で静止）としてユーザビリティを向上。
*   7.  **B2B健康コラムの厳密なフィルタリング [NEW]**
    *   **スポーツ記事の除外**: アスリート向けのコラム（サッカー・投球フォームなど）が混入しないよう、カテゴリ判定で `health` のみを厳密に抽出するクエリフィルタを適用。
*   8.  **ブラウザキャッシュ対策（Metaタグ） [NEW]**
    *   `face_analyzer_business.html` のヘッダーに `no-cache` / `no-store` のキャッシュ制御Metaタグを埋め込み、ブラウザによる古いHTMLファイルの保持を防止。
*   9.  **左右の余白を活かした2カラム分割・ワイドレスポンシブレイアウト [NEW]**
    *   **空白エリアの有効活用**: 測定完了時、空きスペースとなる左カラム（カメラ下部）の `#leftSideAdviceContainer` へセルフケア処方箋カード（`#makeupGuideCard`）をJavaScript DOM操作で動的移設。
    *   **横並びレイアウト（Side-by-side）**: 縦長スクロールを回避するため、ストレッチの「テキスト説明カード」と「SVG矢印付き日本人画像」を `display: flex` で左右50%ずつの横並びにレイアウト。
    *   **ねじり方向の整合性修正（矢印の向き）**: 椅子ひねり写真でモデルが体を左後方（椅子の背もたれ側）へねじっているのに対し、矢印がデスク方向を指していた矛盾を解消。矢印の起点を右側（START: 正面）、先端を左側（END: ねじる）へ反転させ、実際のストレッチ動作と完全に一致するように修正。
    *   **モバイルレスポンシブ**: スマートフォンなどの縦画面時は、メディアクエリ（`max-width: 768px`）により自動的に縦1カラムへフォールバックする設計。��枠には、選択した「なりたい顔」に連動した**メイク化粧品広告（チーク等）**が表示されます。
   * **「🏥 健康チェック」タブから起動した場合:**
     - 「非接触バイタルチェック＆自律神経・ストレス分析」カードから起動して送信すると、測定結果は同じようにレポートされます。
     - 右側の広告枠は美容ではなく**健康管理の観点に切り替わり**、測定されたHRV指標に基づき、低HRV（ストレス高）なら**GABA/ビタミンサプリメント**、通常HRVなら**VAAMスマートフィットウォーター**が動的にレコメンドされます。

---

## カテゴリ別記事フィルタリング＆新規コンテンツ追加（最新アップデート）

全3大領域（スポーツ、健康チェック、美容骨格）において、記事数が大量に増えても読みたいコンテンツに瞬時にアクセスできるよう、UI/UXの最適化を行いました。

### 1. ポータルへのカテゴリ別動的サブタブの導入
各タブパネルの上部にスライド・ホバー対応のサブタブを追加しました。
*   **⚽ スポーツ:** `全て` `サッカー` `野球` `テニス` `バスケ` `バレー` `陸上競技`
*   **🏥 健康チェック:** `全て` `🧘 自律神経・ストレス` `😴 睡眠・回復` `🍏 食事・栄養` `🚶 肩こり・腰痛ケア` `🕒 セルフチェック`
*   **✨ 美容骨格:** `全て` `💄 顔骨格メイク` `🧴 プチプラコスメ` `💇 ヘアスタイル` `🏫 スクールメイク` `📐 ボディメイク`

### 2. 記事カードのカテゴリ属性タグ（data-sport）の実装
*   既存の全記事カード、および新規追加した全ての記事カードに対し、フィルタリング用の属性 `data-sport="[カテゴリ名]"` を設定しました。
*   [portal.js](file:///C:/Users/ke/Desktop/connect-ai-v2/portal.js) 内のフィルタリング処理を拡張し、アクティブなタブパネル内の `.article-card` だけを親ノード（`.tab-panel`）ベースで動的に抽出し、クラス衝突なしに滑らかに非表示・表示を切り替える汎用的ロジックにアップデートしました。

### 3. 中高生・若年女性ターゲット向けの美容新規コンテンツの追加
「美容骨格」タブのフィルタリングを機能させるため、特にターゲット層（10代の学生や若い女性）のニーズに沿ったトレンド記事を4つ新規追加しました。
1.  **💄 顔骨格メイク:** 「【顔タイプ別】丸顔・面長を理想の卵型に変えるコントゥアリング立体メイク術」
2.  **🧴 プチプラコスメ:** 「【プチプラ限定】CANMAKE＆KATEでつくるトレンドの透明感うるみメイク特集」
3.  **🏫 スクールメイク:** 「学校でもバレない！ナチュラルに血色感をアップする「すっぴん風スクールメイク」」
4.  **💇 ヘアスタイル:** 「顔の縦横比アライメントで選ぶ！あなたに最も似合う髪型・前髪デザイン」

---

## 📅 自動記事追加＆デプロイシステム（毎日5記事自動配信）

案A（毎日メインカテゴリーごとに5記事、計15記事を新規投稿）に基づき、サーバー統合も想定した軽量・依存ライブラリなしの自動パブリッシュシステムを構築しました。

### 1. 新規追加・構成ファイル
*   4.  **B2B制御JavaScript (`portal_business.js` / [NEW])**
    *   ログイン・セッション管理、測定データの履歴保持、および `articles_db.json` からB2Bに適した「健康・姿勢・ストレス」関連記事の自動抽出フィルタリングロジック。
*   5.  **日本人モデルによるホログラフィック・アライメントストレッチガイド [NEW]**
    *   **高精度AI写真の生成**: `generate_image` を用い、デスクで実践できる日本人の首こり・体幹・肩こりのストレッチ画像（`stretch_neck_jp.jpg`, `stretch_twist_jp.jpg`, `stretch_shoulder_jp.jpg`）を生成・実装。
    *   **ホログラフィック・グリッド＆矢印演出**: 画像に半透明の測定グリッド、ねじり・引っ張り方向を示す直感的な**SVG製ホログラフィック矢印アニメーション**、および `ALIGNMENT: OK` といったHUD情報をオーバーレイ配置。
    *   **詳細ステップガイド**: ユーザーがストレッチ方法を間違えないよう、**「姿勢・方向」**・**「キープ秒数（左右各15秒）」**・**「呼吸法」**のガイド情報を併記。
*   6.  **呼吸ガイドのゲーミフィケーション（風船アニメーション） [NEW]**
    *   **呼吸同調バルーン**: 4-7-8呼吸法（吸う4秒・止める7秒・吐く8秒）およびリラックス呼吸（吸う3秒・吐く6秒）のタイミングに合わせ、鼓動するように膨張・収縮する**動的ネオンサークル**を開発・実装。
*   7.  **B2B健康コラムの厳密なフィルタリング [NEW]**
    *   **スポーツ記事の除外**: アスリート向けのコラム（サッカー・投球フォームなど）が混入しないよう、カテゴリ判定で `health` のみを厳密に抽出するクエリフィルタを適用。
*   8.  **ブラウザキャッシュ対策（Metaタグ） [NEW]**
    *   `face_analyzer_business.html` のヘッダーに `no-cache` / `no-store` のキャッシュ制御Metaタグを埋め込み、ブラウザによる古いHTMLファイルの保持を防止。
*   9.  **左右の余白を活かした2カラム分割・ワイドレスポンシブレイアウト [NEW]**
    *   **空白エリアの有効活用**: 測定完了時、空きスペースとなる左カラム（カメラ下部）の `#leftSideAdviceContainer` へセルフケア処方箋カード（`#makeupGuideCard`）をJavaScript DOM操作で動的移設。
    *   **横並びレイアウト（Side-by-side）**: 縦長スクロールを回避するため、ストレッチの「テキスト説明カード」と「SVG矢印付き日本人画像」を `display: flex` で左右50%ずつの横並びにレイアウト。
    *   **ねじり・伸展方向の整合性修正（矢印の向き）**: 
        - **椅子ひねり**: 実際の回旋方向（左奥の背もたれ方向）へ矢印の向きを反転。
        - **肩甲骨拡張**: 補助の引き寄せ力ではなく、伸ばしている主動作側の腕の「指先（右側）」へ向かって伸びるように矢印を反転（START: 胸元 ➔ END: 指先を伸ばす）。
    *   **モバイルレスポンシブ**: スマートフォンなどの縦画面時は、メディアクエリ（`max-width: 768px`）により自動的に縦1カラムへフォールバックする設計。
*   10. **エクササイズビジュアルガイドの方向ルールの永続化 [NEW]**
    *   **ルール化**: 今後の機能拡張時にAIエージェントが同じ描画ロジックを逸脱しないよう、本プロジェクトのカスタム設計原則を記述した [exercise_rules.md](file:///C:/Users/ke/Desktop/portal-mockup/.agents/rules/exercise_rules.md) を作成・保存。
*   11. **管理画面統合型 HUDアライメント座標エディタの開発＆連携 [NEW]**
    *   **ドラッグ＆ドロップ座標エディタ**: 管理画面から視覚的にストレッチガイドの矢印を微調整できる [admin_hud_editor.html](file:///C:/Users/ke/Desktop/portal-mockup/admin_hud_editor.html) を開発。開始点・中継点・終了点のアンカーピンをマウス/タッチ操作で直接ドラッグして、2次ベジェ曲線の矢印パスをリアルタイムに再描画・調整可能。
    *   **管理画面への統合**: 管理画面 [admin.html](file:///C:/Users/ke/Desktop/portal-mockup/admin.html) のヘッダーアクション部分に `✨ HUD矢印エディタ` ボタンを配置し、ワンクリックで起動できるように連携。
    *   **リアルタイム反映ロジック**: 確定した座標データを `localStorage` に保存し、測定結果画面 [face_analyzer_business.html](file:///C:/Users/ke/Desktop/portal-mockup/face_analyzer_business.html) およびダッシュボード [portal_business.js](file:///C:/Users/ke/Desktop/portal-mockup/portal_business.js) が起動時に動的読み込みしてSVGオーバーレイを書き換える「ライブ連携機能」を開発・実装。
*   **[articles_db.json](file:///C:/Users/ke/Desktop/portal-mockup/articles_db.json)** : 専門家監修の高品質な記事データ（3日分・計45記事分）を格納したデータベース。
*   **[daily_publisher.py](file:///C:/Users/ke/Desktop/portal-mockup/daily_publisher.py)** : 実行されるたびに本日の追加分を `index.html` に自動挿入し、自動的に `git commit & push` を実行してデプロイするコアスクリプト。
*   **[setup_daily_task.ps1](file:///C:/Users/ke/Desktop/portal-mockup/setup_daily_task.ps1)** : Windows の「タスクスケジューラ」に上記パブリッシャーを毎日午前9:00に自動実行するように登録するPowerShellスクリプト。

### 2. 記事データベースの特徴
挿入される記事カードは、以下の要素が完璧に組み込まれており、デザイン崩れが起きないよう自動制御されます。
*   **動的Unsplash画像:** 各サブカテゴリ（サッカー、睡眠、メイクなど）にマッチした高解像度画像を動的設定。
*   **カテゴリ属性（data-sport）:** 新規記事に対しても正しい絞り込み用タグを付与し、カテゴリボタンでのフィルタリングに対応。
*   **AIカメラフックボタン:** 各記事のテーマに合わせ、適切な測定スキャン（`l_side` 側面測定、`front` 左右荷重測定、`face_rppg` 自律神経測定など）を起動するアクションを搭載。

### 3. 実行方法
*   **手動実行（今すぐ追加する場合）:**
    デスクトップの `portal-mockup` フォルダでコマンドラインを開き、以下を実行します。
    ```bash
    python daily_publisher.py
    ```
*   **毎日自動実行（9:00 AM JST）:**
    管理者権限でPowerShellを開き、`portal-mockup` フォルダ内で以下を実行してタスクスケジューラに登録します。
    ```powershell
    Set-ExecutionPolicy Bypass -Scope Process -Force
    .\setup_daily_task.ps1
    ```

